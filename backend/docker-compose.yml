version: "3.9"

services:
  veg-quality-ai:
    build: .
    container_name: veg-quality-ai
    ports:
      - "20000:20000"
    env_file:
      - backend/.env
    volumes:
      - ./backend/uploads:/app/uploads
      - ./backend/runs:/app/runs
    restart: unless-stopped
